{
  "name": "discord-server-automation-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Project to automate news about several areas and send it to a discord server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.120.0",
    "@cloudflare/workers-types": "^5.20260811.1"
  }
}
